---
paths:
  - "**/*.{ts,tsx}"
---

# TypeScript guidelines

Canonical content (single source of truth, shared with Cursor): @../../coding-rules/rules/typescript.md

If the import above was not expanded into context, read `coding-rules/rules/typescript.md` and follow it when writing or editing TypeScript. It applies on top of `coding-rules/rules/javascript.md`.
