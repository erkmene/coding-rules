---
paths:
  - "**/*.{jsx,tsx}"
---

# React guidelines (React UI code only)

Canonical content (single source of truth, shared with Cursor): @../../coding-rules/rules/react.md

If the import above was not expanded into context, read `coding-rules/rules/react.md` and follow it when writing or editing React components and hooks. Do not apply React patterns to non-React code.
