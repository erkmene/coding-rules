# API design guidelines (always loaded, self-scoped)

Canonical content (single source of truth, shared with Cursor): @../../coding-rules/rules/api-design.md

These rules apply only when designing or implementing HTTP (REST) APIs; skip them for code with no API surface. If the import above was not expanded into context, read `coding-rules/rules/api-design.md` and follow it whenever API work applies.
