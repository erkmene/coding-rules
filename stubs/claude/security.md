# Security guidelines (always loaded)

Canonical content (single source of truth, shared with Cursor): @../../coding-rules/rules/security.md

If the import above was not expanded into context, read `coding-rules/rules/security.md` and follow it for all work in this repository. It extends the universal security items (injection, exec sinks, randomness, secrets); it does not replace them.
