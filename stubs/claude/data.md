# Data & persistence guidelines (always loaded, self-scoped)

Canonical content (single source of truth, shared with Cursor): @../../coding-rules/rules/data.md

These rules apply only to projects with a database or other durable store; skip them otherwise. If the import above was not expanded into context, read `coding-rules/rules/data.md` and follow it for schema, migration, and query work.
